# Changelog: ระบบแจ้งเตือนเครื่องซักผ้า
โปรเจกต์: ระบบแจ้งเตือนเครื่องซักผ้า | กลุ่ม 3

## [v1.0.0] - 2026-08-02 — Release แรกที่ทดสอบสมบูรณ์

### Added
- เพิ่มจุดเริ่มต้นการทำงานของโปรแกรมด้วย `if __name__ == "__main__": main()`

### Verified
- ทดสอบการทำงานครบทั้ง 5 เมนู
- ทดสอบการตรวจสอบเครื่องที่ว่าง
- ทดสอบการตรวจสอบเครื่องของผู้ใช้
- ทดสอบการแสดงสถานะเครื่องทั้งหมด
- ทดสอบการอัปเดตสถานะผ่านระบบ Admin Login
- ทดสอบการออกจากโปรแกรม

---

## [v0.8.0] - 2026-08-02 — เมนูหลักและการเชื่อมต่อฟังก์ชัน

### Added
- สร้างฟังก์ชัน `main()` สำหรับควบคุมเมนูหลักด้วย `while True`
- เชื่อมต่อฟังก์ชันต่างๆ เข้ากับเมนูหลักทั้ง 5 ตัวเลือก
- เมนูอัปเดตสถานะเครื่องเรียก `admin_login()` ก่อนอนุญาตให้แก้ไขข้อมูล

### Fixed
- ดักจับ `ValueError` เมื่อผู้ใช้กรอกข้อมูลที่ต้องเป็นตัวเลขไม่ถูกต้อง
- จบการทำงานของโปรแกรมด้วย `break` เมื่อผู้ใช้เลือกเมนูออกจากโปรแกรม

---

## [v0.7.0] - 2026-08-02 — ระบบยืนยันตัวตนผู้ดูแลร้าน

### Added
- สร้างฟังก์ชัน `admin_login()` สำหรับตรวจสอบ username และ password กับ `ADMIN_ACCOUNT`
- ใช้ Tuple Unpacking ในการแยกข้อมูล username และ password
- จำกัดจำนวนครั้งในการเข้าสู่ระบบตาม `MAX_LOGIN_ATTEMPTS`
- แสดงจำนวนครั้งที่เหลือเมื่อเข้าสู่ระบบไม่สำเร็จ
- ปฏิเสธการเข้าถึงระบบผู้ดูแลเมื่อกรอกข้อมูลผิดครบตามจำนวนที่กำหนด

---

## [v0.6.0] - 2026-08-02 — อัปเดตสถานะเครื่องด้วย Defensive Programming

### Added
- สร้างฟังก์ชัน `update_status()` สำหรับแก้ไขสถานะและเวลาที่เหลือของเครื่อง

### Fixed
- ตรวจสอบสถานะใหม่กับ `VALID_STATUSES` ก่อนบันทึกข้อมูล
- ตรวจสอบหมายเลขเครื่องก่อนเข้าถึงข้อมูล เพื่อป้องกัน `KeyError`

---

## [v0.5.0] - 2026-08-02 — แสดงสถานะเครื่องทั้งหมด

### Added
- สร้างฟังก์ชัน `show_all_status()` สำหรับแสดงสถานะและเวลาที่เหลือของเครื่องซักผ้าทั้งหมด
- จัดรูปแบบการแสดงผลเป็นคอลัมน์เพื่อให้อ่านข้อมูลได้ง่าย

---

## [v0.4.0] - 2026-08-02 — ตรวจสอบเครื่องของผู้ใช้ด้วย Defensive Programming

### Added
- สร้างฟังก์ชัน `check_my_machine()` สำหรับแสดงสถานะและเวลาที่เหลือของเครื่องตามหมายเลขที่ผู้ใช้ระบุ
- เพิ่มข้อความแจ้งเตือนที่แตกต่างกันตามสถานะของเครื่อง

### Fixed
- ป้องกัน `KeyError` โดยตรวจสอบว่า `machine_id` มีอยู่ใน `machines_dict` ก่อนเข้าถึงข้อมูล

---

## [v0.3.0] - 2026-08-02 — คำนวณเครื่องว่างด้วย Set

### Added
- สร้างฟังก์ชัน `get_available_machines()` โดยใช้ Set Comprehension เพื่อค้นหาและคืนค่าหมายเลขเครื่องที่มีสถานะ "ว่าง"
- สร้างฟังก์ชัน `check_available()` สำหรับแสดงจำนวนและหมายเลขเครื่องที่ว่าง โดยเรียงลำดับด้วย `sorted()`

---

## [v0.2.0] - 2026-08-02 — โครงสร้างข้อมูลเครื่องซักผ้า

### Added
- สร้าง `machines` เป็น `dictionary` สำหรับเก็บข้อมูลเครื่องซักผ้า โดยใช้หมายเลขเครื่องเป็น key และสถานะกับเวลาที่เหลือเป็น value
- กำหนดข้อมูลเริ่มต้นของเครื่องซักผ้าทั้ง 10 เครื่อง

---

## [v0.1.0] - 2026-08-02 — โครงสร้างข้อมูลคงที่ของระบบ

### Added
- กำหนด `VALID_STATUSES` เป็น `frozenset` สำหรับเก็บสถานะที่ระบบยอมรับ 5 สถานะ โดยสมาชิกภายในไม่สามารถเพิ่ม ลบ หรือแก้ไขได้
- กำหนด `ADMIN_ACCOUNT` เป็น `tuple` สำหรับเก็บ username และ password ของผู้ดูแลร้าน
- กำหนด `MAX_LOGIN_ATTEMPTS = 3` สำหรับจำกัดจำนวนครั้งในการกรอกรหัสผ่านผิด
